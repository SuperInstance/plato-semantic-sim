[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"
[project]
name = "plato-semantic-sim"
version = "0.1.0"
description = "Jaccard and cosine semantic similarity"
license = {text = "MIT"}
requires-python = ">=3.8"
