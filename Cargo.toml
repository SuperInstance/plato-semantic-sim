[package]
name = "plato-semantic-sim"
version = "0.1.0"
edition = "2021"
description = "Semantic similarity engine — cosine, Euclidean, Jaccard, batch comparison, clustering"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
